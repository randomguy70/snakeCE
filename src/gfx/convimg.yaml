palettes:
  - name: palette
    images: automatic
    fixed-entries:
      - image: palette.png
converts:
  - name: myimages
    palette: palette
    images:
      - palette.png
outputs:
  - type: c
    include-file: gfx.h
    converts:
      - myimages
    palettes:
      - palette